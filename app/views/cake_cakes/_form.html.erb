<%= form_with(model: cake_cake, local: true) do |form| %>
  <% if cake_cake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cake_cake.errors.count, "error") %> prohibited this cake_cake from being saved:</h2>

      <ul>
      <% cake_cake.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :cake_cake_name %>
  </div>

  <div class="field">
    <%= form.label :ingredients %>
    <%= form.text_area :ingredients, id: :cake_cake_ingredients %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
